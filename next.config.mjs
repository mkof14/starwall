/** @type {import('next').NextConfig} */
const nextConfig = {
  allowedDevOrigins: [
    "localhost",
    "127.0.0.1",
    "*.cursor.com",
    "*.cursor.sh",
    "*.cursorusercontent.com",
  ],
};

export default nextConfig;
