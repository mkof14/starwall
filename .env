# Local defaults for Prisma CLI (does not load .env.local).
# Vercel / production overrides these with the Neon URLs.
DATABASE_URL=postgresql://starwall:starwall@127.0.0.1:5432/starwall
DIRECT_URL=postgresql://starwall:starwall@127.0.0.1:5432/starwall
